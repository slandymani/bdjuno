table:
  name: account_balance
  schema: public
select_permissions:
  - permission:
      allow_aggregations: true
      columns:
        - address
        - loki_balance
        - mgeo_balance
        - all_balances
        - height
      filter: {}
    role: anonymous