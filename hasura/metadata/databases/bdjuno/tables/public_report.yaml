table:
  name: report
  schema: public
select_permissions:
  - permission:
      allow_aggregations: true
      columns:
        - id
        - validator
        - oracle_script_id
        - tx_hash
      filter: {}
    role: anonymous